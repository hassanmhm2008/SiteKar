# 🚀 راهنمای سریع - سیستم مدیریت کار و درخواست

## 📌 خلاصه تغییرات

سیستم به یک معماری **حرفه‌ای با پایگاه داده MySQL** تبدیل شده است:

### ✅ تغییرات اصلی:

1. **جداسازی کامل منوها:**
   - ✓ کارها (Tasks) - با سیستم تایید
   - 📦 درخواست‌ها (Requests) - با انتخاب از لیست کالاها
   - 📋 مدیریت کالاها (فقط ادمین)
   - 🏢 مدیریت واحدها (فقط ادمین)

2. **سیستم واحدهای سازمانی:**
   - هر واحد یک رئیس دارد
   - کاربران به واحدها تخصیص می‌یابند
   - ساختار سلسله مراتبی کامل

3. **پایگاه داده MySQL:**
   - تمام اطلاعات در SQL ذخیره می‌شود
   - 7 جدول اصلی: users, departments, tasks, requests, request_items, products, comments

4. **Backend API با Node.js:**
   - RESTful API با Express.js
   - مدیریت ایمن اتصالات
   - پشتیبانی کامل از عملیات CRUD

5. **سیستم تایید کار:**
   - کاربر کار ایجاد می‌کند → وضعیت: "در انتظار تایید"
   - تحویل‌گیرنده تایید/رد می‌کند
   - پس از تایید، کار در لیست تحویل‌گیرنده ظاهر می‌شود

## 🛠️ نصب سریع (3 مرحله)

### مرحله 1: دیتابیس
```bash
# در MySQL یا phpMyAdmin
mysql -u root -p < database.sql
```

### مرحله 2: Backend
```powershell
cd backend
npm install
npm start
```
✅ سرور روی http://localhost:3000 اجرا می‌شود

### مرحله 3: Frontend
- فایل `index.html` را با Live Server یا http-server اجرا کنید
- ورود با: `admin` / `admin123`

## 📂 ساختار فایل‌ها

```
sitekar/
├── backend/                    # Backend API
│   ├── routes/                # مسیرهای API
│   │   ├── auth.js           # احراز هویت
│   │   ├── users.js          # کاربران
│   │   ├── departments.js    # واحدها
│   │   ├── tasks.js          # کارها
│   │   ├── requests.js       # درخواست‌ها
│   │   └── products.js       # کالاها
│   ├── db.js                 # اتصال MySQL
│   ├── server.js             # سرور اصلی
│   ├── package.json
│   └── .env                  # تنظیمات دیتابیس
│
├── index.html                # صفحه ورود
├── dashboard.html            # داشبورد
├── tasks.html                # مدیریت کارها
├── requests.html             # مدیریت درخواست‌ها
├── products.html             # مدیریت کالاها (ادمین)
├── departments.html          # مدیریت واحدها (ادمین)
├── users.html                # مدیریت کاربران
│
├── styles.css                # استایل
├── app.js                    # توابع عمومی
├── api.js                    # توابع ارتباط با API
│
├── database.sql              # Schema دیتابیس
├── INSTALL.md                # راهنمای کامل نصب
└── README.md                 # توضیحات پروژه
```

## 🎯 جریان کاری سیستم

### 1️⃣ ایجاد کاربر
```
ادمین → مدیریت کاربران → افزودن کاربر
↓
- انتخاب واحد
- انتخاب رئیس/سرپرست
- تعیین نقش
```

### 2️⃣ ایجاد کار
```
کاربر → کارها → ایجاد کار
↓
- انتخاب تحویل‌گیرنده
- وضعیت: "در انتظار تایید"
↓
تحویل‌گیرنده → مشاهده کارهای در انتظار → تایید/رد
↓
- اگر تایید شود → کار در لیست تحویل‌گیرنده ظاهر می‌شود
- اگر رد شود → با دلیل رد، به ایجادکننده نمایش داده می‌شود
```

### 3️⃣ ایجاد درخواست کالا
```
کاربر → درخواست‌ها → ایجاد درخواست
↓
- انتخاب کالا از لیست
- تعیین تعداد
- افزودن چندین کالا
↓
ثبت درخواست → شماره درخواست صادر می‌شود
```

### 4️⃣ مدیریت کالاها (فقط ادمین)
```
ادمین → مدیریت کالاها
↓
- افزودن/ویرایش/حذف کالا
- دسته‌بندی کالاها
- فعال/غیرفعال کردن
```

## 📊 Schema دیتابیس

### جدول users
- اطلاعات کاربران
- واحد متعلق (department_id)
- سرپرست (supervisor_id)
- نقش (role)

### جدول departments
- نام واحد
- رئیس واحد (manager_id)
- توضیحات

### جدول tasks
- عنوان و توضیحات
- اولویت و وضعیت
- ایجادکننده (created_by)
- تحویل‌گیرنده (assigned_to)
- تاییدکننده (approved_by)

### جدول requests
- شماره درخواست یکتا
- کاربر درخواست‌دهنده
- وضعیت
- تاریخ ایجاد/تایید

### جدول request_items
- آیتم‌های هر درخواست
- کالا (product_id)
- تعداد
- توضیحات

### جدول products
- کد و نام کالا
- دسته‌بندی
- واحد
- وضعیت فعال/غیرفعال

### جدول comments (آینده)
- نظرات و تاریخچه

## 🔐 نقش‌های کاربری

| نقش | دسترسی‌ها |
|-----|----------|
| **مدیر** | دسترسی کامل - مدیریت واحدها، کالاها، کاربران، کارها، درخواست‌ها |
| **رئیس واحد** | مدیریت کارها و کاربران واحد خود |
| **کارمند** | ایجاد کار، ایجاد درخواست، مشاهده کارهای خود |
| **کارشناس** | ایجاد کار، ایجاد درخواست، مشاهده کارهای خود |

## 🆕 ویژگی‌های جدید

✅ **جداسازی کارها و درخواست‌ها**  
✅ **سیستم واحدهای سازمانی**  
✅ **مدیریت کالاها توسط ادمین**  
✅ **پایگاه داده MySQL**  
✅ **Backend API با Express.js**  
✅ **سیستم تایید کار**  
✅ **درخواست با چند کالا**  
✅ **شماره یکتا برای درخواست‌ها**  
✅ **فیلتر پیشرفته**  

## 🐛 رفع مشکلات رایج

### خطای اتصال به دیتابیس
```bash
# بررسی MySQL
net start MySQL80

# بررسی اطلاعات .env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=YOUR_PASSWORD
DB_NAME=sitekar_db
```

### خطای CORS
```
Backend باید روی http://localhost:3000 اجرا باشد
```

### منوی کالاها و واحدها نمایش داده نمی‌شود
```
فقط کاربر با نقش "مدیر" به این بخش دسترسی دارد
```

## 📞 تست سریع API

```powershell
# تست سلامت سرور
curl http://localhost:3000/api/health

# ورود
curl -X POST http://localhost:3000/api/auth/login `
  -H "Content-Type: application/json" `
  -d '{"username":"admin","password":"admin123"}'

# دریافت کاربران
curl http://localhost:3000/api/users

# دریافت کالاها
curl http://localhost:3000/api/products
```

## 📈 مراحل بعدی (پیشنهادی)

1. ✅ Hash کردن رمز عبورها با bcrypt
2. ✅ اضافه کردن JWT Authentication
3. ✅ سیستم گزارش‌گیری
4. ✅ نوتیفیکیشن برای تاییدکنندگان
5. ✅ آپلود فایل برای کارها
6. ✅ چت/کامنت برای کارها
7. ✅ تقویم و یادآوری
8. ✅ نمودار و آمار پیشرفته

## 💡 نکات مهم

- 🔒 رمز عبور پیش‌فرض را حتماً تغییر دهید
- 📁 از دیتابیس Backup بگیرید
- 🔐 در Production از HTTPS استفاده کنید
- 🚀 برای Performance، Connection Pooling فعال است
- 📊 لاگ‌های سرور را مانیتور کنید

---

**✨ سیستم آماده استفاده است!**

برای سوالات بیشتر به `INSTALL.md` مراجعه کنید.
