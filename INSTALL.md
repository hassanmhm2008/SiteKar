# 🚀 راهنمای نصب و راه‌اندازی سیستم مدیریت کار و درخواست

این راهنما مراحل کامل نصب و راه‌اندازی سیستم را شرح می‌دهد.

## 📋 پیش‌نیازها

قبل از شروع، مطمئن شوید موارد زیر را نصب کرده‌اید:

1. **Node.js** (نسخه 16 یا بالاتر)
   - دانلود از: https://nodejs.org/
   - بررسی نصب: `node --version`

2. **MySQL Server** (نسخه 8.0 یا بالاتر)
   - دانلود از: https://dev.mysql.com/downloads/mysql/
   - یا استفاده از XAMPP/WAMP

3. **مرورگر وب مدرن** (Chrome, Firefox, Edge)

## 🛠️ مرحله 1: نصب پایگاه داده

### روش 1: استفاده از phpMyAdmin (XAMPP/WAMP)

1. XAMPP یا WAMP را اجرا کنید
2. Apache و MySQL را استارت کنید
3. به آدرس `http://localhost/phpmyadmin` بروید
4. فایل `database.sql` را Import کنید:
   - Tab "Import" را انتخاب کنید
   - فایل `database.sql` را انتخاب کنید
   - دکمه "Go" را کلیک کنید

### روش 2: استفاده از MySQL Command Line

```bash
# ورود به MySQL
mysql -u root -p

# اجرای فایل SQL
source path/to/database.sql

# یا در ویندوز
mysql -u root -p < database.sql
```

## 🔧 مرحله 2: نصب Backend

1. به پوشه backend بروید:
```powershell
cd backend
```

2. نصب وابستگی‌ها:
```powershell
npm install
```

3. تنظیم فایل `.env`:
- فایل `.env` را باز کنید
- اطلاعات دیتابیس خود را وارد کنید:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=YOUR_PASSWORD
DB_NAME=sitekar_db
DB_PORT=3306

PORT=3000
NODE_ENV=development
```

4. اجرای سرور:
```powershell
# حالت عادی
npm start

# یا حالت توسعه (با auto-restart)
npm run dev
```

سرور باید روی `http://localhost:3000` اجرا شود.

## 🌐 مرحله 3: راه‌اندازی Frontend

### روش 1: استفاده از Live Server (توصیه می‌شود)

1. افزونه Live Server را در VS Code نصب کنید
2. روی فایل `index.html` راست‌کلیک کنید
3. گزینه "Open with Live Server" را انتخاب کنید

### روش 2: استفاده از http-server

```powershell
# نصب http-server
npm install -g http-server

# اجرا در پوشه اصلی پروژه
http-server -p 8080
```

سپس به `http://localhost:8080` بروید.

### روش 3: با Python

```powershell
# Python 3
python -m http.server 8080
```

## 🔐 ورود اولیه به سیستم

پس از راه‌اندازی، به آدرس Frontend بروید و با اطلاعات زیر وارد شوید:

- **نام کاربری:** `admin`
- **رمز عبور:** `admin123`

## 📁 ساختار پروژه

```
sitekar/
├── backend/                  # Backend API
│   ├── routes/              # API Routes
│   ├── db.js                # اتصال به دیتابیس
│   ├── server.js            # سرور اصلی
│   ├── package.json
│   └── .env                 # تنظیمات محیطی
├── index.html               # صفحه ورود
├── dashboard.html           # داشبورد
├── tasks.html               # مدیریت کارها
├── requests.html            # مدیریت درخواست‌ها
├── users.html               # مدیریت کاربران
├── products.html            # مدیریت کالاها (ادمین)
├── departments.html         # مدیریت واحدها (ادمین)
├── styles.css               # استایل‌ها
├── app.js                   # توابع عمومی
├── api.js                   # توابع API
├── database.sql             # فایل دیتابیس
└── README.md
```

## ✅ ویژگی‌های سیستم

### 👤 سیستم کاربری
- ورود و خروج امن
- نقش‌های مختلف (مدیر، رئیس واحد، کارمند، کارشناس)
- مدیریت واحدها
- تعیین سرپرست برای هر کاربر

### 📋 مدیریت کارها
- ایجاد کار توسط کاربران
- سیستم تایید کار توسط تحویل‌گیرنده
- کارهای تایید شده در لیست کاربر نمایش داده می‌شود
- فیلتر بر اساس وضعیت و اولویت
- تعیین مهلت انجام

### 📦 مدیریت درخواست‌ها
- ایجاد درخواست از لیست کالاها
- انتخاب چند کالا با تعداد
- مشاهده جزئیات درخواست
- سیستم تایید و رد

### 🛍️ مدیریت کالاها (ویژه ادمین)
- افزودن/ویرایش/حذف کالاها
- دسته‌بندی کالاها
- کد و نام یکتا برای هر کالا

### 🏢 مدیریت واحدها (ویژه ادمین)
- ایجاد واحدهای سازمانی
- تعیین رئیس برای هر واحد
- انتساب کاربران به واحدها

## 🔍 عیب‌یابی

### خطای اتصال به دیتابیس

```
❌ خطا در اتصال به پایگاه داده
```

**راه‌حل:**
1. مطمئن شوید MySQL در حال اجراست
2. اطلاعات `.env` را بررسی کنید
3. دسترسی کاربر به دیتابیس را چک کنید

### خطای CORS

```
Access to fetch blocked by CORS policy
```

**راه‌حل:**
- Backend باید اجرا باشد
- آدرس API در `api.js` را بررسی کنید (باید `http://localhost:3000/api` باشد)

### Port در حال استفاده

```
Error: listen EADDRINUSE: address already in use :::3000
```

**راه‌حل:**
```powershell
# پیدا کردن پروسه
netstat -ano | findstr :3000

# کشتن پروسه
taskkill /PID <PID> /F

# یا تغییر port در .env
PORT=3001
```

## 📱 دسترسی از سایر دستگاه‌ها

برای دسترسی از موبایل یا دستگاه دیگر در شبکه محلی:

1. IP کامپیوتر خود را پیدا کنید:
```powershell
ipconfig
```

2. در فایل `api.js` آدرس را تغییر دهید:
```javascript
const API_BASE_URL = 'http://YOUR_IP:3000/api';
```

3. فایروال را تنظیم کنید که Port 3000 را باز کند

## 🔒 نکات امنیتی

⚠️ **برای استفاده در محیط Production:**

1. رمز عبورها را Hash کنید (bcrypt استفاده شود)
2. JWT برای احراز هویت اضافه کنید
3. HTTPS فعال کنید
4. SQL Injection Prevention را پیاده کنید
5. Rate Limiting اضافه کنید
6. Environment Variables را امن نگه دارید

## 📞 پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های سرور را بررسی کنید
2. Console مرورگر را چک کنید (F12)
3. Connection string دیتابیس را بررسی کنید

## 📝 تغییرات نسبت به نسخه قبل

### ✨ ویژگی‌های جدید:
- ✅ جداسازی منوی کارها و درخواست‌ها
- ✅ سیستم مدیریت کالاها
- ✅ سیستم واحدهای سازمانی
- ✅ ارتباط با پایگاه داده MySQL
- ✅ Backend API با Express.js
- ✅ سیستم تایید کار توسط تحویل‌گیرنده
- ✅ ایجاد کار برای خود، مدیر و اعضای گروه

---

**نسخه:** 2.0.0  
**تاریخ:** 2025-11-01  
**توسعه‌دهنده:** سیستم مدیریت کار و درخواست
