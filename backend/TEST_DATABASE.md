# 🧪 راهنمای تست دیتابیس

بعد از اینکه دیتابیس را با phpMyAdmin import کردید، می‌توانید با این اسکریپت‌ها سلامت دیتابیس را بررسی کنید.

## 🚀 نحوه استفاده

### مرحله 1: نصب وابستگی‌ها (اگر هنوز نکرده‌اید)

```powershell
cd E:\Projects\sitekar\backend
npm install
```

### مرحله 2: اجرای تست سریع

```powershell
npm run test:quick
```

این دستور یک تست سریع انجام می‌دهد و اطلاعات زیر را نشان می‌دهد:
- ✅ وضعیت اتصال
- 📊 تعداد رکوردها در هر جدول
- 👥 لیست کاربران
- 🏢 لیست واحدها
- 📦 نمونه کالاها

**خروجی نمونه:**
```
🔍 تست سریع دیتابیس...

✓ اتصال به دیتابیس موفق

📊 آمار دیتابیس:
   واحدها: 4
   کاربران: 1
   کالاها: 10
   کارها: 0
   درخواست‌ها: 0

👥 لیست کاربران:
   - مدیر سیستم (admin) - مدیر

🏢 لیست واحدها:
   - مدیریت: واحد مدیریت اصلی
   - فروش: واحد فروش و بازاریابی
   - فنی: واحد فنی و پشتیبانی
   - مالی: واحد مالی و حسابداری

📦 نمونه کالاها:
   - P001: کاغذ A4 (لوازم اداری)
   - P002: قلم آبی (لوازم اداری)
   - P003: پوشه (لوازم اداری)
   - P004: دفتر (لوازم اداری)
   - P005: پاکن (لوازم اداری)

✅ دیتابیس کاملاً سالم است و آماده استفاده!
```

### مرحله 3: اجرای تست کامل (اختیاری)

```powershell
npm run test:db
```

این دستور یک تست کامل و جامع انجام می‌دهد:

1. ✅ تست اتصال به MySQL
2. ✅ بررسی وجود دیتابیس sitekar_db
3. ✅ بررسی وجود تمام جداول (7 جدول)
4. ✅ بررسی ساختار جدول departments
5. ✅ بررسی ساختار جدول users
6. ✅ بررسی ساختار جدول products
7. ✅ بررسی ساختار جدول tasks
8. ✅ بررسی ساختار جدول requests
9. ✅ بررسی داده‌های اولیه
10. ✅ بررسی Foreign Keys
11. ✅ بررسی Indexes
12. ✅ تست عملیات Insert و Delete
13. ✅ بررسی Character Set

**خروجی موفق:**
```
=== شروع تست دیتابیس ===

1. تست اتصال به MySQL...
✓ اتصال به MySQL موفق

2. بررسی وجود دیتابیس sitekar_db...
✓ دیتابیس sitekar_db وجود دارد

3. بررسی جداول...
✓ جدول departments موجود است
✓ جدول users موجود است
✓ جدول products موجود است
✓ جدول tasks موجود است
✓ جدول requests موجود است
✓ جدول request_items موجود است
✓ جدول comments موجود است

... (و ادامه تست‌ها)

==================================================
✓ تمام تست‌ها با موفقیت انجام شد!
دیتابیس شما آماده استفاده است.
==================================================
```

## 🐛 رفع خطاها

### خطا: "Cannot find module 'mysql2'"

**راه‌حل:**
```powershell
cd backend
npm install
```

### خطا: "Can't connect to MySQL server"

**راه‌حل:**
1. مطمئن شوید XAMPP اجرا شده
2. MySQL در XAMPP استارت باشد (سبز)
3. به `http://localhost/phpmyadmin` بروید و ببینید باز می‌شود

### خطا: "Access denied for user 'root'"

**راه‌حل:**
فایل `.env` را بررسی کنید:
```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=         # اگر رمز ندارید، خالی بگذارید
DB_NAME=sitekar_db
DB_PORT=3306
```

### خطا: "Unknown database 'sitekar_db'"

**راه‌حل:**
دیتابیس import نشده است. دوباره از phpMyAdmin import کنید.

## ✅ چک‌لیست قبل از تست

- [ ] XAMPP نصب شده
- [ ] MySQL در XAMPP استارت شده (سبز)
- [ ] فایل database.sql در phpMyAdmin import شده
- [ ] فایل `.env` در پوشه backend موجود است
- [ ] وابستگی‌ها نصب شده (`npm install`)

## 🎯 مراحل بعدی

بعد از اینکه تست‌ها موفق بودند:

1. **اجرای Backend:**
   ```powershell
   npm start
   ```
   
2. **تست API:**
   - به `http://localhost:3000/api/health` بروید
   - باید پیام `{"status":"OK"}` را ببینید

3. **اجرای Frontend:**
   - فایل `index.html` را باز کنید
   - با `admin` / `admin123` وارد شوید

## 📊 آمار انتظاری

بعد از import موفق database.sql:

| جدول | تعداد رکورد |
|------|-------------|
| departments | 4 |
| users | 1 |
| products | 10 |
| tasks | 0 |
| requests | 0 |
| request_items | 0 |
| comments | 0 |

اگر این اعداد را می‌بینید، نصب موفق بوده است! ✅

---

**موفق باشید! 🎉**
