# 🔧 رفع خطای اتصال به MySQL

## ❌ خطای شما:
```
ERROR 2003 (HY000): Can't connect to MySQL server on 'localhost:3306' (10061)
```

این یعنی MySQL Server در حال اجرا نیست.

---

## ✅ راه‌حل‌ها (از ساده به سخت):

### 🥇 روش 1: استفاده از phpMyAdmin (بهترین روش)

#### مرحله 1: نصب XAMPP
1. اگر XAMPP ندارید، از اینجا دانلود کنید:
   👉 https://www.apachefriends.org/download.html
2. نصب کنید (Next, Next, ...)

#### مرحله 2: استارت MySQL
1. **XAMPP Control Panel** را باز کنید
2. روی دکمه **Start** کنار **MySQL** کلیک کنید
3. صبر کنید تا پس‌زمینه سبز شود

#### مرحله 3: Import دیتابیس
1. مرورگر را باز کنید
2. به این آدرس بروید: `http://localhost/phpmyadmin`
3. از تب بالا **Import** را انتخاب کنید
4. روی **Choose File** کلیک کنید
5. فایل `database.sql` را از پوشه پروژه انتخاب کنید
6. پایین صفحه روی **Go** کلیک کنید
7. منتظر بمانید تا کامل شود

✅ **تمام!** دیتابیس نصب شد!

---

### 🥈 روش 2: استفاده از MySQL Command Line

#### مرحله 1: استارت MySQL
همان روش 1، مرحله 2 را انجام دهید

#### مرحله 2: اجرای دستور
```powershell
# اگر XAMPP دارید:
C:\xampp\mysql\bin\mysql -u root < "E:\Projects\sitekar\database.sql"

# اگر رمز دارید:
C:\xampp\mysql\bin\mysql -u root -p < "E:\Projects\sitekar\database.sql"
```

---

### 🥉 روش 3: استارت MySQL Service

#### اگر MySQL مستقل نصب کرده‌اید:

1. کلیدهای **Win + R** را بزنید
2. `services.msc` تایپ کنید و Enter
3. **MySQL80** (یا MySQL) را پیدا کنید
4. راست‌کلیک → **Start**

یا از Command Prompt (Run as Administrator):
```powershell
net start MySQL80
```

---

## 🧪 تست اتصال

بعد از استارت MySQL، تست کنید:

### در phpMyAdmin:
1. به `http://localhost/phpmyadmin` بروید
2. اگر صفحه باز شد، MySQL کار می‌کند ✅

### در Command Line:
```powershell
C:\xampp\mysql\bin\mysql -u root -e "SELECT VERSION();"
```

اگر نسخه MySQL را نشان داد، کار می‌کند ✅

---

## 📋 چک‌لیست نصب

- [ ] XAMPP نصب شده است
- [ ] XAMPP Control Panel باز است
- [ ] MySQL در XAMPP استارت شده (سبز)
- [ ] phpMyAdmin باز می‌شود (http://localhost/phpmyadmin)
- [ ] فایل database.sql در phpMyAdmin Import شده
- [ ] دیتابیس `sitekar_db` ایجاد شده
- [ ] جدول `users` وجود دارد

---

## 🚀 مرحله بعد: راه‌اندازی Backend

بعد از نصب دیتابیس:

```powershell
# به پوشه backend بروید
cd E:\Projects\sitekar\backend

# نصب وابستگی‌ها
npm install

# تنظیم .env (بررسی کنید اطلاعات درست باشد)
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=        (خالی اگر رمز ندارید)
# DB_NAME=sitekar_db

# اجرای سرور
npm start
```

اگر این پیام را دیدید، موفق بوده‌اید:
```
✅ اتصال به پایگاه داده MySQL برقرار شد
🚀 Server is running on http://localhost:3000
```

---

## 🆘 همچنان مشکل دارید؟

### بررسی Port:
ممکن است Port 3306 اشغال باشد:

```powershell
netstat -ano | findstr :3306
```

### بررسی فایروال:
ممکن است فایروال MySQL را بلاک کرده باشد.

### پیام خطا را کپی کنید:
اگر خطای دیگری گرفتید، پیام کامل خطا را بفرستید.

---

## 💡 نکته مهم:

**بعد از هر بار که کامپیوتر را Restart می‌کنید:**
- باید XAMPP Control Panel را باز کنید
- MySQL را دوباره Start کنید

**یا می‌توانید MySQL را به عنوان Service نصب کنید:**
در XAMPP Control Panel → کلیک روی علامت ❌ کنار MySQL → انتخاب "Install as Service"

---

**موفق باشید! 🎉**
