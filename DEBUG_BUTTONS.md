# راهنمای عیب‌یابی دکمه‌های department-requests

## مشکل: دکمه‌ها کار نمی‌کنند

### مرحله 1: بررسی کنسول مرورگر

1. صفحه `department-requests.html` را باز کنید
2. کلید `F12` را بزنید تا Developer Tools باز شود
3. به تب **Console** بروید
4. صفحه را Refresh کنید (`Ctrl+F5`)

### مرحله 2: بررسی لاگ‌ها

وقتی صفحه لود می‌شود، باید این پیام‌ها را ببینید:
- `Initializing menu...`
- `Loading department requests...`

### مرحله 3: کلیک روی دکمه‌ها

#### دکمه "👁️" (نمایش):
وقتی کلیک می‌کنید باید ببینید:
```
viewDetails called with requestId: <شماره>
Details fetched: {success: true, request: {...}}
```

#### دکمه "✓" (تایید):
وقتی کلیک می‌کنید باید ببینید:
```
openApproveModal called with requestId: <شماره>
Modal should be open now
```

#### دکمه "✗" (رد):
وقتی کلیک می‌کنید باید ببینید:
```
openRejectModal called with requestId: <شماره>
Reject modal should be open now
```

---

## خطاهای احتمالی:

### خطا 1: `Uncaught ReferenceError: openApproveModal is not defined`
**علت:** تابع قبل از لود شدن DOM فراخوانی شده
**راه حل:** مطمئن شوید که `<script>` در انتهای `<body>` قرار دارد

### خطا 2: `Cannot read property 'classList' of null`
**علت:** Modal در HTML وجود ندارد یا ID اشتباه است
**راه حل:** بررسی کنید که:
```html
<div id="approveModal" class="modal">
<div id="rejectModal" class="modal">
```
در HTML وجود دارند

### خطا 3: `Failed to fetch`
**علت:** سرور backend در حال اجرا نیست
**راه حل:** 
```bash
cd E:\Projects\sitekar\backend
node server.js
```

### خطا 4: هیچ لاگی نمی‌بینید
**علت:** JavaScript اصلاً اجرا نمی‌شود
**راه حل:**
1. بررسی کنید که فایل `app.js` و `api.js` لود شده‌اند
2. در تب **Network** ببینید که آیا این فایل‌ها با خطا لود شده‌اند

---

## تست سریع:

در کنسول مرورگر این دستورات را اجرا کنید:

```javascript
// بررسی وجود توابع
typeof openApproveModal
// باید بگوید: "function"

typeof viewDetails  
// باید بگوید: "function"

// بررسی وجود modal ها
document.getElementById('approveModal')
// باید element را برگرداند، نه null

document.getElementById('rejectModal')
// باید element را برگرداند، نه null

// بررسی currentUser
JSON.parse(localStorage.getItem('currentUser'))
// باید اطلاعات کاربر را نشان دهد
```

---

## اگر هنوز کار نمی‌کند:

1. **Cache را پاک کنید:**
   - `Ctrl + Shift + Delete`
   - یا `Ctrl + F5` برای Hard Refresh

2. **بررسی Network:**
   - تب Network را باز کنید
   - ببینید آیا API ها با خطا صدا زده می‌شوند

3. **بررسی سرور:**
   - مطمئن شوید سرور روی پورت 3000 اجرا است
   - در مرورگر بروید: `http://localhost:3000/api/requests/all/pending`
   - باید JSON برگرداند

4. **Screenshot از Console:**
   - اگر هیچ لاگی نمی‌بینید، screenshot بگیرید و ارسال کنید
   - اگر خطا می‌بینید، متن کامل خطا را کپی کنید

---

## نتیجه‌گیری:

بعد از این تغییرات، وقتی روی دکمه‌ها کلیک می‌کنید باید:
1. لاگ‌ها در Console ظاهر شوند
2. Modal ها باز شوند
3. عملیات تایید/رد انجام شود

**اگر هیچ لاگی نمی‌بینید** = JavaScript اصلاً اجرا نمی‌شود (مشکل در لود فایل)
**اگر لاگ می‌بینید اما modal باز نمی‌شود** = مشکل در CSS یا HTML
**اگر modal باز می‌شود اما submit کار نمی‌کند** = مشکل در API یا سرور
